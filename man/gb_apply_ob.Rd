% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimism_bias.R
\name{gb_apply_ob}
\alias{gb_apply_ob}
\title{Apply an optimism bias uplift to a cost stream}
\usage{
gb_apply_ob(values, ob_pct, mitigation = 0)
}
\arguments{
\item{values}{Numeric vector of baseline cost values.}

\item{ob_pct}{Numeric scalar. Optimism bias percentage as a
decimal. Pass \code{gb_optimism_bias(category)} to use the published
upper bound.}

\item{mitigation}{Numeric scalar in \verb{[0, 1]}. Fraction of the
upper bound that has been mitigated through project definition
and risk management. Default \code{0} (no mitigation; full upper
bound applied).}
}
\value{
A numeric vector the same length as \code{values}, with the
uplift applied.
}
\description{
Applies an OB uplift, optionally with a mitigation factor that
represents progress on risk identification and management:
\code{cost_with_ob = cost_baseline * (1 + ob_pct * (1 - mitigation))}.
}
\examples{
costs <- c(100, 50, 50)
ob <- gb_optimism_bias("non_standard_buildings")
gb_apply_ob(costs, ob)
gb_apply_ob(costs, ob, mitigation = 0.5)
}
\references{
HM Treasury (2003). Supplementary Green Book Guidance:
Optimism Bias, Annex A2 on mitigation factors.
}
\seealso{
\code{\link[=gb_optimism_bias]{gb_optimism_bias()}}, \code{\link[=gb_appraise]{gb_appraise()}}.

Other optimism bias: 
\code{\link{gb_categories}()},
\code{\link{gb_optimism_bias}()}
}
\concept{optimism bias}
