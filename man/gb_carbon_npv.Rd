% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/carbon.R
\name{gb_carbon_npv}
\alias{gb_carbon_npv}
\title{Net present value of an emissions path}
\usage{
gb_carbon_npv(
  emissions,
  years,
  scenario = "central",
  series = "traded",
  schedule = "standard",
  base_year = NULL,
  sign = "cost"
)
}
\arguments{
\item{emissions}{Numeric vector of emissions per year, in tCO2e
(positive = emitted, negative = avoided / abated).}

\item{years}{Integer vector of years matching \code{emissions}.}

\item{scenario}{One of \code{"low"}, \code{"central"} (default), \code{"high"}.}

\item{series}{One of \code{"traded"} (default), \code{"non_traded"}.}

\item{schedule}{One of \code{"standard"} (default), \code{"health"},
\code{"catastrophic"}.}

\item{base_year}{Optional integer base year for monetary values
(e.g. \code{2024}). If supplied, carbon values are rebased via
\code{gb_deflator()}.}

\item{sign}{Character. \code{"cost"} (default) treats positive
\code{emissions} as a cost (negative cashflow); \code{"benefit"} treats
positive as avoided emissions (positive cashflow).}
}
\value{
A \code{gb_appraisal} object.
}
\description{
Multiplies an emissions vector (tCO2e per year) by the DESNZ
carbon value at each year, then discounts under the kinked STPR.
Returns a \code{gb_appraisal} object.
}
\examples{
# 100 tCO2e emitted each year from 2024 to 2030
emissions <- rep(100, 7)
years <- 2024:2030
gb_carbon_npv(emissions, years, base_year = 2024)
}
